import { getUserActiveCartService } from "./getUserCart.service";

export const addLineItemToCartService = async (authToken: string) => {
    try {
        
    } catch (error) {
        throw new Error(`Error adding item to the cart: ${error}`)
    }
}